# Android Performance Report

**Device / Emulator:** None detected via `adb devices`.

**Result:** PERFORMANCE MEASUREMENT PENDING – no on‑device inference could be executed.

*Reason:* No connected Android device or running emulator was found at the time of validation, therefore latency, CPU/memory usage, and APK size measurements could not be obtained.

---
*If a device becomes available later, re‑run the performance measurement script (`scripts/android_performance.py`) to capture real latency statistics and update this report.*
