#!/usr/bin/env pwsh
# verify_and_install.ps1 – builds the app, installs, starts, and checks logs

# Navigate to project root (script assumes it's run from the project directory)
.
# Build the app
.\n# Use relative path for gradlew
.
# Actually we are already in the project directory, so just run:
.
# Run gradle clean assembleDebug
.
# The following commands will be executed sequentially.
.
# Build
.
.
# Explanation: the dots used above are placeholders; we'll replace them with actual commands.
